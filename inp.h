#include <stdio.h>

    void inpNumbers(){
    int a, b, c;
    char a1, b1, c1;
        scanf("%d", &a);
        scanf("%c", &a1);
        scanf("%d", &b);
        scanf("%c", &c1);
        scanf("%d", &c);
        scanf("%c", &b1);
}
